nacos:
  address: 172.224.4.189:8848,172.224.5.90:8848,172.224.7.140:8848
spring:
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_ADDRESS:${nacos.address}}
      config:
        server-addr: ${NACOS_ADDRESS:${nacos.address}}
        file-extension: yaml
        ext-config:
          - data-id: gray.yaml
            refresh: true

